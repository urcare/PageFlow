# PageFlow

- Use React + Vite + TypeScript + Tailwind CSS for the frontend.
- Use Node.js + Express + TypeScript for the backend.
- Keep Supabase secrets server-side and use environment variables.
- Prefer focused, mobile-first UI changes consistent with the green and white design system.
- Run `npm run build` after implementation changes.
